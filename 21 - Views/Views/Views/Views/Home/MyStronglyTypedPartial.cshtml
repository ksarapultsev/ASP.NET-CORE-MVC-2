@model IEnumerable<string>
<div class="bg-info">
    This is the message from the partioal view.
    <ul>
        @foreach (string str in Model)
        {
            <li>@str</li>
        }
    </ul>
</div>